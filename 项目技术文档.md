# 智能桌面气象站 — 项目技术文档

## 一、项目概述

基于 STM32F103C8T6 + FreeRTOS 的多任务嵌入式系统，集成多传感器采集、OLED 显示、自动控制、蓝牙遥控、WiFi 上报、Flash 日志存储和多级报警功能。

- **MCU:** STM32F103C8T6 (ARM Cortex-M3, 72MHz, 64KB Flash, 20KB RAM)
- **RTOS:** FreeRTOS 10.3.1 (CMSIS OS2 封装)
- **构建工具:** CMake + Ninja + arm-none-eabi-gcc
- **开发环境:** STM32CubeMX + VS Code

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                   FreeRTOS 调度器                     │
├───────────┬───────────┬───────────┬─────────────────┤
│ SensorTask│ OLEDTask  │ BT_Task   │ WiFi_Task       │
│ (High)    │(AboveNml) │(AboveNml) │ (AboveNml)      │
│ 1s 周期    │ 200ms     │ 阻塞接收   │ 30s 周期        │
├───────────┼───────────┼───────────┼─────────────────┤
│ AlarmTask │ ButtonTask│ LoggerTask│ defaultTask     │
│(AboveNml) │(AboveNml) │ (Normal)  │ (AboveNml)      │
│ 1s 周期    │ 10ms      │ 60s 周期  │ 100ms 周期      │
└───────────┴───────────┴───────────┴─────────────────┘
            │                    │
      ┌─────┴──────┐      ┌─────┴──────┐
      │ g_state    │      │ UART1      │
      │ 全局状态    │      │ 互斥量共享  │
      │(SystemState)│      │ (BT+Debug) │
      └────────────┘      └────────────┘
```

### 2.2 全局数据共享

所有任务通过 `SystemState_t g_state`（定义在 `User/Inc/system_state.h`）共享数据：

```c
typedef struct {
  // 传感器数据
  float temp;           // 温度 (°C)
  float humi;           // 湿度 (%)
  uint16_t light_raw;   // 光敏 ADC 原始值
  int16_t pitch;        // 俯仰角 (°)
  int16_t roll;         // 横滚角 (°)

  // 控制参数
  float temp_threshold; // 温度阈值
  uint8_t display_page; // OLED 页面号 (0~3)

  // 执行器状态
  uint8_t fan_speed;    // 风扇转速 0~100%
  uint16_t servo_angle; // 舵机角度 0~180°

  // 系统信息
  SystemMode_t mode;    // AUTO / MANUAL / SLEEP
  AlarmLevel_t alarm;   // NONE / WARN / CRITICAL
  uint16_t log_count;   // 日志记录数
  uint32_t uptime_sec;  // 运行时间
  uint32_t free_heap;   // 剩余堆空间
} SystemState_t;
```

### 2.3 硬件引脚分配

| 引脚         | 外设             | 功能       | 通信方式             |
| ------------ | ---------------- | ---------- | -------------------- |
| PA0          | DHT11            | 温湿度     | 单总线 (软件)        |
| PA1          | 光敏电阻         | 光照强度   | ADC1                 |
| PA2/PA3      | ESP8266          | WiFi       | USART2               |
| PA6          | TB6612 PWMA      | 风扇 PWM   | TIM3 CH1             |
| PA7/PA8      | TB6612 AIN1/AIN2 | 风扇方向   | GPIO                 |
| PA9/PA10     | HC-06            | 蓝牙       | USART1               |
| PA11         | SG-90            | 舵机 PWM   | TIM1 CH4             |
| PA15         | W25Q64 CS        | Flash 片选 | GPIO                 |
| PB0/PB1      | 编码器 A/B       | 旋转检测   | EXTI                 |
| PB2          | 编码器 SW        | 按键       | GPIO                 |
| PB3/PB4/PB5  | W25Q64           | Flash 数据 | SPI1                 |
| PB6/PB7      | OLED             | 显示屏     | 软件 I2C (地址 0x78) |
| PB8/PB9/PC13 | LED (绿/黄/红)   | 状态指示   | GPIO                 |
| PB10/PB11    | MPU6050          | 姿态传感器 | I2C2                 |
| PB12/PB13    | KEY1/KEY2        | 用户按键   | GPIO                 |
| PB14         | 蜂鸣器           | 报警       | GPIO                 |

---

## 三、传感器驱动实现

### 3.1 DHT11 温湿度传感器

**文件:** `User/Src/sensor.c` — `DHT11_Read()`

**原理:** 单总线协议，主机发开始信号后，从机返回 40bit 数据（湿度整数+湿度小数+温度整数+温度小数+校验和）。

**实现要点：**

1. **微秒级延时:** 使用 TIM2 做微秒计数器（PSC=71, 72MHz/72=1MHz → 1us/跳），不受 FreeRTOS SysTick 影响
2. **关中断保护:** 读取 40bit 数据前调用 `__disable_irq()`，防止中断干扰时序
3. **直接寄存器读引脚:** `DHT11_GPIO_Port->IDR & DHT11_Pin` 比 HAL_GPIO_ReadPin 快
4. **分步超时检测:** 每个等待步骤独立超时，通过 `dht11_debug_code` 定位失败原因

```
时序流程:
主机拉低20ms → 释放 → 等DHT11拉低(80us) → 等DHT11拉高(80us) →
等DHT11拉低(50us) → 读bit(28us后判断电平) → 重复40次 → 校验
```

### 3.2 MPU6050 六轴姿态传感器

**文件:** `User/Src/sensor.c` — `MPU6050_Init()` / `MPU6050_Init()`

**原理:** I2C 通信读取加速度计数据，用 `atan2f` + `sqrtf` 计算俯仰角和横滚角。

```c
void MPU6050_Read(void) {
  uint8_t buf[14];
  HAL_I2C_Mem_Read(&hi2c2, MPU6050_ADDR, 0x3B, 1, buf, 14, 100);
  int16_t ax = (buf[0] << 8) | buf[1];
  int16_t ay = (buf[2] << 8) | buf[3];
  int16_t az = (buf[4] << 8) | buf[5];

  g_state.pitch = atan2f(-ax, sqrtf(ay * ay + az * az)) * (180.0f / M_PI);
  g_state.roll  = atan2f(ay, az) * (180.0f / M_PI);
}
```

- I2C 地址: `0x68 << 1 = 0xD0`（HAL 要求 8bit 地址）
- 初始化: 写 `0x00` 到 `PWR_MGMT_1` (0x6B) 唤醒传感器
- 读取寄存器: `0x3B` 开始连续读 14 字节（AX/AY/AZ/GX/GY/GZ 温度）

### 3.3 光敏传感器

**文件:** `User/Src/sensor.c` — `LightSensor_Read()`

```c
void LightSensor_Read(void) {
  HAL_ADC_Start(&hadc1);
  HAL_ADC_PollForConversion(&hadc1, 100);
  g_state.light_raw = HAL_ADC_GetValue(&hadc1);
  HAL_ADC_Stop(&hadc1);
}
```

- ADC1 通道 1 (PA1)，12bit 精度 (0~4095)
- 光敏 ADC 值越小 = 光照越强（分压电路特性）

---

## 四、执行器控制

### 4.1 风扇控制 (TB6612 + 直流电机)

**文件:** `User/Src/motor.c` — `fan_control()`

**控制逻辑 (AUTO 模式):**

| 温度范围          | 风扇转速 | PWM 占空比   |
| ----------------- | -------- | ------------ |
| T < 阈值          | 0% (停)  | 0            |
| 阈值 ≤ T < 阈值+4 | 30%      | duty × 9/100 |
| T ≥ 阈值+4        | 60%      | duty × 9/100 |

- TIM3 CH1 (PA6) 输出 PWM，频率由 TIM3 预分频决定
- AIN1=HIGH, AIN2=LOW 确定正转方向
- `*9/100` 是占空比映射系数，适配 TB6612 的 PWM 响应范围

### 4.2 舵机控制 (SG-90)

**文件:** `User/Src/motor.c` — `SG90_Control()`

**控制逻辑 (AUTO 模式):**

| 光敏 ADC 值 | 光照强度 | 舵机角度 |
| ----------- | -------- | -------- |
| < 1000      | 强光     | 180°     |
| 1000~3000   | 适中     | 90°      |
| ≥ 3000      | 暗光     | 0°       |

- TIM1 CH4 (PA11) 输出 PWM
- 舵机角度 → PWM 占空比公式: `500 + (angle × 2000) / 180`
  - 0° → 500us (0.5ms)
  - 90° → 1500us (1.5ms)
  - 180° → 2500us (2.5ms)

---

## 五、蓝牙遥控实现

**文件:** `User/Src/BT_WIFI.c` — `BT_Control()` + `BT_ParseCommand()`

### 5.1 接收机制

`BT_Task` 以 `AboveNormal` 优先级阻塞在 `HAL_UART_Receive(&huart1, &byte, 1, osWaitForever)`，逐字节接收，以 `\n` 或 `\r` 作为命令结束符。

### 5.2 指令集

| 指令      | 格式           | 功能                                     |
| --------- | -------------- | ---------------------------------------- |
| FAN       | `FAN:30`       | 设置风扇转速 (0-100)，自动切 MANUAL 模式 |
| SERVO     | `SERVO:90`     | 设置舵机角度 (0-180)，自动切 MANUAL 模式 |
| STATUS    | `STATUS`       | 返回温湿度光照数据                       |
| STOP      | `STOP`         | 停止风扇                                 |
| THRESHOLD | `THRESHOLD:35` | 设置温度阈值                             |
| LOG:DUMP  | `LOG:DUMP`     | 导出全部 Flash 日志                      |
| LOG:CLEAR | `LOG:CLEAR`    | 清空日志                                 |

### 5.3 UART1 共享机制

蓝牙接收和调试输出 (DebugPrint) 共用 USART1，通过 `UART1_MutexHandle` 互斥量保护：

```c
void DebugPrint(const char *msg) {
  if (osMutexAcquire(UART1_MutexHandle, 50) == osOK) {  // 50ms 短超时
    HAL_UART_Transmit(&huart1, (uint8_t *)msg, strlen(msg), 200);
    osMutexRelease(UART1_MutexHandle);
  }
}
```

蓝牙回复使用 `osWaitForever` 确保送达。

---

## 六、WiFi 上报实现

**文件:** `User/Src/BT_WIFI.c` — `ESP8266_Init()` + `ESP8266_Report()`

### 6.1 AT 指令驱动架构

核心函数 `ESP8266_SendCmd` 封装了"发送 AT 指令 → 逐字节接收 → 匹配期望响应"的流程：

```c
static uint8_t ESP8266_SendCmd(const char *cmd, const char *expect, uint32_t timeout) {
  // 1. 清空接收缓冲区
  // 2. 通过 UART2 发送 AT 指令 + \r\n
  // 3. 循环读取 UART2 响应，拼接到 esp_rx_buf[512]
  // 4. 用 strstr 检查是否包含期望字符串
  // 5. 超时返回 1，匹配成功返回 0
}
```

### 6.2 ESP8266 初始化流程

```
排空UART2 → AT+RST重启(等3秒) → AT测试(最多3次) → ATE0关闭回显
→ AT+CWMODE=1 设Station → AT+CWJAP连WiFi → AT+CIFSR查IP
```

每个步骤通过 `ESP_DIAG` 宏直接写 UART1 输出诊断信息。

### 6.3 数据上报

```
AT+CIPSTART="TCP","服务器IP",8080  → 建立TCP连接
AT+CIPSEND=<长度>                   → 进入发送模式
JSON数据                            → 通过UART2发送
等 "SEND OK"                        → 确认发送完成
AT+CIPCLOSE                         → 关闭连接
```

上报 JSON 格式:

```json
{ "temp": 25, "humi": 65, "light": 3060, "fan": 30 }
```

---

## 七、W25Q64 Flash 日志系统

### 7.1 SPI Flash 驱动

**文件:** `User/Src/w25q64.c`

W25Q64 是 64KB SPI Flash，通过 SPI1 (PB3/PB4/PB5) 连接，CS=PA15。

**基本操作：**

| 操作     | 指令码 | 说明                       |
| -------- | ------ | -------------------------- |
| 读数据   | 0x03   | 3字节地址 + 连续读取       |
| 页编程   | 0x02   | 写使能后，最多写256字节/页 |
| 擦除扇区 | 0x20   | 擦除4KB扇区                |
| 写使能   | 0x06   | 写操作前必须发             |
| 读状态   | 0x05   | 轮询 WIP 位等待写完成      |

**SPI 通信流程（以读数据为例）：**

```c
W25Q64_CS_LOW();                          // 拉低片选
uint8_t cmd[4] = {0x03, addr>>16, addr>>8, addr};  // 指令 + 3字节地址
HAL_SPI_Transmit(&hspi1, cmd, 4, 100);    // 发送指令和地址
HAL_SPI_Receive(&hspi1, buf, len, 1000);  // 接收数据
W25Q64_CS_HIGH();                         // 释放片选
```

**写保护处理:** 每次写操作前发 `WriteEnable` (0x06)，写入后轮询 `ReadStatus` (0x05) 等待 WIP 位清零。

### 7.2 日志存储系统

**文件:** `User/Src/logger.c`

**日志记录结构体 (10 字节/条):**

```c
typedef struct {
  uint16_t temp;       // 温度 ×10 (如 253 表示 25.3°C)
  uint16_t humi;       // 湿度 ×10
  uint16_t light;      // 光照 ADC 原始值
  int16_t  pitch;      // 俯仰角
  uint16_t timestamp;  // 开机后分钟数
} LogRecord_t;
```

**存储布局:** 使用 W25Q64 的第一个 Sector (4KB)，地址 0x00000~0x00FFF：

```
Sector 0 (4096 bytes):
┌──────┬──────┬──────┬─────┬──────────┐
│ Rec0 │ Rec1 │ Rec2 │ ... │ Rec408   │
│ 10B  │ 10B  │ 10B  │     │ 10B      │
└──────┴──────┴──────┴─────┴──────────┘
容量: 4096 / 10 = 409 条记录
```

**初始化流程 (`Logger_Init`):**

1. 从地址 0 开始逐条读取，检查 `timestamp == 0xFFFF`（擦除后的初始值）
2. 找到第一条空记录的位置，即为写入起点
3. 如果已满 (≥409条)，擦除整个 Sector，从头开始

**写入流程 (`Logger_Write`):**

1. 从 `g_state` 提取数据填充 `LogRecord_t`
2. `W25Q64_PageWrite` 写入当前地址
3. 地址 += 10，记录数 +1
4. 满 409 条后擦除 Sector 循环覆盖

**导出 (`Logger_DumpToBT`):** 遍历所有记录，格式化为字符串通过 DebugPrint（UART1）发送到蓝牙。

---

## 八、报警系统

**文件:** `User/Src/alarm.c`

### 8.1 报警规则

| 报警类型 | 条件                   | 级别     | 蜂鸣器     | LED             |
| -------- | ---------------------- | -------- | ---------- | --------------- |
| 温度过高 | 温度 ≥ 阈值 (默认35°C) | WARN     | 500ms 脉冲 | 黄灯常亮        |
| 倾斜     | pitch/roll > 30°       | WARN     | 500ms 脉冲 | 黄灯常亮        |
| 双重     | 温度过高 + 倾斜        | CRITICAL | 500ms 脉冲 | 红灯快闪(200ms) |
| 正常     | 无异常                 | NONE     | 关         | 绿灯常亮        |

### 8.2 实现

```c
void Alarm_Check(void) {
  // 1. 检查温度 (×10 比较避免浮点)
  if ((uint16_t)(g_state.temp * 10) >= temp_threshold_x10) temp_high = 1;

  // 2. 检查倾斜 (绝对值比较)
  int16_t abs_pitch = g_state.pitch < 0 ? -g_state.pitch : g_state.pitch;
  if (abs_pitch > 30 || abs_roll > 30) tilt = 1;

  // 3. 确定报警级别
  // 4. 控制蜂鸣器 (500ms 切换 GPIO)
  // 5. 更新 g_state.alarm 供 LED 任务使用
}
```

蜂鸣器和 LED 均为 **active-high**（HIGH=响/亮，LOW=关/灭）。

---

## 九、用户交互

### 9.1 OLED 显示 (4 页)

**页面 0 — 实时传感器数据:**

```
行1: T:25.3C H:65.0%     (温湿度)
行2: Light: 3060         (光敏值)
行3: P:12  R:-5          (俯仰/横滚)
行4: AUTO    OK          (模式 + 报警)
```

**页面 1 — 阈值 + 执行器:**

```
行1: Thres:35.0C         (温度阈值)
行2: Fan: 30%            (风扇转速)
行3: Servo: 90           (舵机角度)
行4: Mode: AUTO          (系统模式)
```

**页面 2 — 日志信息:**

```
行1: === LOG ===
行2: Count: 15           (当前记录数)
行3: Max: 409            (最大容量)
行4: Bt:LOG to dump      (操作提示)
```

**页面 3 — 系统信息:**

```
行1: === SYS ===
行2: Up: 15min           (运行时间)
行3: Heap: 12288         (总堆大小)
行4: Free: 4200          (剩余堆)
```

### 9.2 编码器 + 按键

| 操作       | 功能                                |
| ---------- | ----------------------------------- |
| 旋转编码器 | 调节温度阈值 (±0.5°C, 范围 15~35°C) |
| 编码器按钮 | 重置温度阈值为 28°C                 |
| KEY1       | 切换模式 AUTO → MANUAL → SLEEP      |
| KEY2       | 切换 OLED 显示页面 (0→1→2→3→0)      |

### 9.3 LED 状态灯

| 系统状态        | 绿灯 | 黄灯 | 红灯       | 蜂鸣器     |
| --------------- | ---- | ---- | ---------- | ---------- |
| 正常            | 常亮 | 灭   | 灭         | 关         |
| 警告 (WARN)     | 灭   | 常亮 | 灭         | 500ms 脉冲 |
| 严重 (CRITICAL) | 灭   | 灭   | 200ms 快闪 | 500ms 脉冲 |

---

## 十、关键问题与解决

### 10.1 JTAG 引脚冲突

**问题:** PB3/PB4/PB5 默认被 JTAG 占用，SPI1 无法配置。

**解决:** 在 `main.c` 中，`MX_SPI1_Init()` **之前**调用：

```c
__HAL_AFIO_REMAP_SWJ_NOJTAG();  // 释放 PB3/PB4/PB5，保留 SWD
```

### 10.2 FreeRTOS 堆内存不足

**问题:** 默认 `configTOTAL_HEAP_SIZE=8192` 字节，8 个任务栈总计需 ~9728 字节 + TCB ~1080 字节 ≈ 10808 字节。WiFi_Task 是最后一个创建的，内存耗尽导致 `osThreadNew` 返回 NULL，任务未启动。

**解决:** 在 `FreeRTOSConfig.h` 中改为 `configTOTAL_HEAP_SIZE=12288`。

### 10.3 任务优先级饿死

**问题:** WiFi_Task 和 LoggerTask 设为 `BelowNormal`，被 2 个 `Realtime` + 多个 `AboveNormal` 任务完全饿死，拿不到 CPU。

**解决:** 全部任务最高不超过 `AboveNormal`，WiFi_Task 和 LoggerTask 提到 `Normal` 以上。

### 10.4 Active-high 硬件极性

**问题:** LED 和蜂鸣器硬件为 active-high（HIGH=亮/响），代码初始按 active-low 编写，导致绿灯灭、红黄灯亮、蜂鸣器常响。

**解决:** 反转所有 GPIO 写入逻辑：`GPIO_PIN_SET` = 灭/关，`GPIO_PIN_RESET` = 亮/响。

---

## 十一、文件结构

```
Core/
├── Src/main.c              — 系统初始化、外设配置、调度器启动
├── Src/freertos.c          — 任务定义与创建、defaultTask LED控制
├── Src/gpio.c              — GPIO 引脚配置
├── Src/tim.c               — 定时器 PWM 配置
├── Src/usart.c             — UART1/UART2 配置
├── Src/adc.c               — ADC1 配置
├── Src/i2c.c               — I2C2 配置
├── Src/spi.c               — SPI1 配置
└── Inc/FreeRTOSConfig.h    — FreeRTOS 配置 (堆大小12KB, 栈溢出检测)
User/
├── Src/sensor.c            — DHT11 + MPU6050 + 光敏驱动
├── Src/motor.c             — TB6612 风扇 + SG90 舵机控制
├── Src/BT_WIFI.c           — 蓝牙指令解析 + ESP8266 WiFi 驱动
├── Src/alarm.c             — 多级报警逻辑
├── Src/logger.c            — W25Q64 日志系统
├── Src/Encoder.c           — 编码器 + 按键处理
├── Src/oled.c              — SSD1306 OLED 驱动
├── Src/w25q64.c            — SPI Flash 底层驱动
├── Src/system_state.c      — 全局状态初始化
└── Src/delay.c             — 微秒/毫秒延时
```
