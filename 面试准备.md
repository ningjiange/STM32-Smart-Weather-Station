# 面试准备 — 项目实际考察篇

本文档站在 HR/技术面试官的角度，梳理两个项目中**最可能被追问的点**，以及如何回答。

---

## 项目一：基于 STM32 和 FreeRTOS 的智能桌面气象站

### 1. 一句话介绍项目

> 基于 STM32F103C8T6 + FreeRTOS 的 8 任务多线程系统，集成 DHT11/MPU6050/光敏传感器采集、OLED 四页显示、TB6612 风扇 PWM 调速、SG-90 舵机控制、HC-06 蓝牙遥控、ESP8266 WiFi TCP 上报、W25Q64 Flash 日志存储和多级报警系统。

### 2. 面试官必问 & 参考回答

#### Q1: 为什么选 FreeRTOS？裸机轮询不行吗？

**答：** 这个项目有 8 个并发任务——传感器采集、OLED 刷新、蓝牙接收、WiFi 上报、日志写入等，它们的执行周期和实时性要求各不相同。裸机轮询只能用延时函数来切换任务，无法保证实时响应（比如蓝牙接收必须及时处理，否则丢数据）。FreeRTOS 的抢占式调度器让高优先级任务能立即响应，低优先级任务在空闲时执行，代码结构也更清晰，每个任务独立维护。

#### Q2: FreeRTOS 堆内存怎么算的？为什么从 8KB 改到 12KB？

**答：** FreeRTOS 所有任务栈 + TCB（任务控制块）都从 `configTOTAL_HEAP_SIZE` 这个堆池里分配。我逐个计算了 8 个任务的栈需求：

```
SensorTask:  1536B（atan2f+sqrtf 数学函数调用链深）
OLEDTask:    1280B（4页显示缓冲）
defaultTask: 2048B（LED控制+系统初始化）
WiFi_Task:   2048B（ESP8266 AT指令+JSON拼装）
BT_Task:     1024B（蓝牙指令解析）
LoggerTask:   768B（Flash写入）
ButtonTask:    512B
AlarmTask:     512B
合计栈:       9728B
TCB开销:      ~1080B（9个×~120B）
总计:        ~10800B > 8192B
```

原来设的 8KB 不够，最后一个创建的 WiFi_Task 因为内存不足，`osThreadNew` 返回 NULL 但没有报错，任务根本没启动。这是最难排查的问题之一——日志里看不到任何 WiFi 输出，一开始以为是优先级问题，排查了很多轮才发现是内存问题。

#### Q3: 任务优先级怎么设计的？遇到过饿死问题吗？

**答：** 最初把 OLEDTask 和 defaultTask 设为 Realtime（最高优先级），WiFi_Task 和 LoggerTask 设为 BelowNormal。结果 Realtime 任务从不主动让出 CPU，BelowNormal 任务完全拿不到时间片，WiFi 完全不工作。

后来全部调整为：1 个 High（SensorTask）+ 6 个 AboveNormal + 1 个 Normal（WiFi/Logger）。所有任务都有 `osDelay` 会主动让出 CPU，同优先级之间靠时间片轮转。这个经验告诉我：**Realtime 优先级要慎用，除非任务确实不能容忍任何延迟。**

#### Q4: DHT11 单总线协议怎么实现的？为什么关中断？

**答：** DHT11 是单总线时序协议，读取 40bit 数据时对微秒级时序要求很严格——每个 bit 的判断窗口只有几十微秒。FreeRTOS 的 SysTick 中断每 1ms 触发一次，如果在读取过程中被打断，时序就会乱。所以我用 TIM2 做了一个独立的微秒计数器（PSC=71 → 1MHz → 1us/跳），在读取 40bit 数据前调用 `__disable_irq()` 关中断，读完再 `__enable_irq()` 恢复。

这也说明了一个关键点：**FreeRTOS 任务里的延时不能用 `HAL_Delay()`**（它依赖 SysTick，会被 RTOS 调度影响），要用 `osDelay()` 或独立的硬件定时器。

#### Q5: UART1 蓝牙和调试输出共用，怎么解决冲突？

**答：** 蓝牙接收和 DebugPrint 共用 USART1。如果 DebugPrint 用 `osWaitForever` 等互斥量，可能阻塞蓝牙接收导致丢指令。我的方案是：DebugPrint 获取互斥量时用 **50ms 短超时**，拿不到就放弃输出（调试信息丢了不影响功能）；蓝牙回复则用 `osWaitForever` 确保指令一定能送达。

#### Q6: 传感器数据共享怎么保证一致性？

**答：** 所有任务通过全局结构体 `g_state` 共享数据。写入端只有 SensorTask（1s 周期），读取端有 OLEDTask、WiFi_Task、AlarmTask、LoggerTask 等多个任务。在单核 STM32 上，单个赋值操作是原子的（比如 `g_state.temp = 25.3`），所以不需要加锁。但如果多个字段需要同时读取（比如 temp+humi+light 要打包成 JSON），需要确保读取期间写入端不会更新。我通过 `g_state` 的读写时序设计（写入端只在采集周期末尾一次性更新所有字段）来规避这个问题，没有引入额外的互斥量开销。

#### Q7: W25Q64 Flash 日志怎么设计的？

**答：** 用 W25Q64 的第一个 Sector（4KB），每条记录 10 字节（温度×10、湿度×10、光照、姿态角、时间戳），最多存 409 条。初始化时从地址 0 开始逐条读取，找到 `timestamp == 0xFFFF`（擦除后的初始值）的位置就是写入起点。满了就擦除整个 Sector 从头开始，实现循环覆盖。每次写入前先发 WriteEnable (0x06)，写完后轮询 ReadStatus (0x05) 等待 WIP 位清零，确保写入完成。

#### Q8: 项目里最困难的问题是什么？

**答：** 最难的是排查 WiFi_Task 为什么完全不工作。症状是：蓝牙 app 里看不到任何 WiFi 相关日志，任务似乎根本没运行。排查过程经历了 4 轮：

1. **第一轮**：以为是优先级问题，把 WiFi 从 BelowNormal 提到 Normal → 没解决
2. **第二轮**：以为是 DebugPrint 互斥量问题，改为直接写 UART1 → 没解决
3. **第三轮**：ESP8266_Init 加了逐段诊断，确认连诊断消息都没输出 → 任务本身没运行
4. **第四轮**：查 FreeRTOSConfig.h，发现 `configTOTAL_HEAP_SIZE=8192`，8 个任务栈总计需要 ~11KB → 堆不够，WiFi_Task 作为最后一个创建的任务，`osThreadNew` 返回 NULL → 改为 12288 后解决

这个问题教会我：**嵌入式系统里"没有输出"比"有错误输出"更难排查，要从最底层（内存、时钟、硬件）往上查。**

---

## 项目二：基于 ESP32 和 LVGL 的多功能智能手表

### 1. 一句话介绍项目

> 基于 ESP32 + FreeRTOS + LVGL 8.4 的智能手表，集成 MPU6050 姿态检测、ILI9341 TFT 彩屏显示、加速度峰值检测运动算法、WiFi NTP 时间同步/天气 API/Web Server、BLE 指令控制、W25Q64 Flash 数据记录。

### 2. 面试官必问 & 参考回答

#### Q1: 互补滤波是什么？为什么不用卡尔曼滤波？

**答：** 加速度计在静态时准确但动态时有噪声，陀螺仪动态响应好但有漂移。互补滤波把两者结合：短期信任陀螺仪（高通），长期信任加速度计（低通）。公式是 `angle = 0.98 × (angle + gyro × dt) + 0.02 × accel_angle`。

不用卡尔曼的原因：这个项目只需要显示角度，不需要预测和估计，互补滤波计算量小（只有一行公式），在 ESP32 上 10ms 采样周期内轻松完成，效果已经够用。如果要做四旋翼飞控这种高精度场景，才会考虑卡尔曼。

#### Q2: LVGL 8.4 里的 lv_canvas 画 3D 立方体怎么实现的？

**答：** 3D 立方体有 8 个顶点，每个顶点是 3D 坐标。流程是：

1. MPU6050 读取 Pitch/Roll → 构建旋转矩阵（绕 X 轴和 Y 轴旋转）
2. 8 个顶点 × 旋转矩阵 → 得到旋转后的 3D 坐标
3. 透视投影：`screen_x = x × fov / (z + fov)`，`screen_y = y × fov / (z + fov)`，把 3D 映射到 2D
4. 在 `lv_canvas` 上用 Bresenham 画线算法连接 12 条边
5. 每 33ms（30fps）刷新一次

LVGL 的 canvas 提供了 `lv_canvas_draw_line` 和 `lv_canvas_fill_bg` 等基础 API，我只需要管 3D 数学计算和连线逻辑。

#### Q3: ESP32 的双核怎么用的？

**答：** ESP32 有两个 Xtensa LX6 核心（Core 0 和 Core 1）。我把 LVGL 渲染和显示刷新放在 Core 1（Display_Task），把 I2C 读取 MPU6050、WiFi、BLE 放在 Core 0。这样 LVGL 的渲染不会被 I2C 通信阻塞，帧率更稳定。FreeRTOS 的 `xTaskCreatePinnedToCore` 可以指定任务绑定到哪个核心。

#### Q4: WiFi 和 I2C 通信冲突是怎么回事？

**答：** ESP32 的 WiFi 和 I2C 都用到了中断，如果 WiFi 的中断处理时间太长，会抢占 I2C 的时序，导致 MPU6050 读取数据出错。解决方法是调整 FreeRTOS 的中断优先级——WiFi 中断优先级设为 5（在 `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY` 以下），I2C 中断优先级设为更低数值（更高优先级），确保 I2C 时序不被打断。

#### Q5: 运动检测（步态识别）怎么实现的？

**答：** 基于加速度峰值检测算法：

1. 每 10ms 读取一次加速度三轴数据
2. 计算合成加速度：`total = sqrt(ax² + ay² + az²)`
3. 减去重力分量（约 9.8），得到动态加速度
4. 设置阈值 0.8g，当动态加速度超过阈值时标记为"迈步"
5. 两次迈步之间最小间隔 300ms（过滤抖动）
6. 根据步频判断状态：静止（<0.5步/秒）/ 走路（0.5~2步/秒）/ 跑步（>2步/秒）

#### Q6: 这个项目和气象站项目有什么区别？为什么要做两个？

**答：** 气象站侧重**多外设集成和系统调度**——8 种外设、8 个任务、多种通信协议，体现的是系统工程能力。智能手表侧重**算法和 UI**——姿态解算、运动检测、LVGL 彩屏界面、WiFi Web Server，体现的是算法实现和用户体验设计能力。

两个项目覆盖的技能点互补：气象站用 STM32 + CubeMX，手表用 ESP-IDF；气象站用字符 OLED，手表用 LVGL 彩屏；气象站用 UART 蓝牙，手表用 BLE。面试时可以根据对方岗位侧重介绍。

---

## 高频追问清单

### 基础概念（必背）

| 问题 | 要点 |
|------|------|
| FreeRTOS 任务栈和堆的区别 | 栈=每个任务的工作台（局部变量+返回地址），堆=OS内存池（TCB+栈空间都从这里分配） |
| 互斥量和信号量的区别 | 互斥量=二值，用于保护共享资源（谁加锁谁解锁）；信号量=计数，用于任务间同步 |
| 任务优先级怎么决定的 | 实时性要求高的给高优先级，但不能都给最高，否则低优先级任务饿死 |
| configTOTAL_HEAP_SIZE 太小会怎样 | 后创建的任务分配不到内存，osThreadNew 返回 NULL，任务不运行 |
| DMA 是什么 | 直接内存访问，不需要 CPU 参与数据搬运，释放 CPU 去做其他事 |

### 硬件外设（按需）

| 问题 | 要点 |
|------|------|
| SPI 和 I2C 的区别 | SPI=全双工、4线、速度快、有片选；I2C=半双工、2线、多设备、有地址 |
| PWM 占空比怎么控制风扇转速 | 占空比越大，平均电压越高，转速越快。TIM3 CH1 输出 PWM，占空比 = duty/100 × period |
| 软件 I2C 和硬件 I2C 的区别 | 硬件 I2C 由外设控制器自动处理时序，CPU 开销小；软件 I2C 用 GPIO 模拟，灵活但占 CPU |
| UART 通信的帧格式 | 起始位(1) + 数据位(8) + 校验位(可选) + 停止位(1)，波特率决定速度 |
| ADC 采样精度 | 12bit ADC → 0~4095，对应 0~3.3V。STM32F103 ADC 采样时间可配置（1.5~239.5 cycles） |

### 算法相关（加分项）

| 问题 | 要点 |
|------|------|
| 互补滤波公式 | `angle = α × (angle + gyro × dt) + (1-α) × accel_angle`，α 通常取 0.98 |
| atan2f 和 atan 的区别 | atan2f 返回完整四象限角度（-π~π），atan 只返回第一四象限（-π/2~π/2） |
| 透视投影公式 | `screen_x = x × fov / (z + fov)`，fov=视场角，z 越远物体越小 |
| 峰值检测算法 | 超过阈值标记一次，两次之间设最小间隔过滤抖动 |

---

## 面试话术模板

### 开场自我介绍（30秒）

> 面试官您好，我是 XXX，XX 大学嵌入式方向的学生。我独立完成过两个开源嵌入式项目：一个是基于 STM32 + FreeRTOS 的智能桌面气象站，涉及 8 任务多线程调度、6 种传感器驱动、蓝牙/WiFi 双无线通信；另一个是基于 ESP32 + LVGL 的智能手表，实现了姿态检测、运动算法和彩色 UI。两个项目都在 GitHub 上开源。

### 项目介绍模板（1分钟）

> 这个项目是 [一句话概括]。
> 技术栈是 [芯片] + [RTOS] + [关键外设]。
> 核心功能包括 [3 个最重要的功能]。
> 遇到的最大问题是 [一个具体的坑]，最终通过 [怎么解决的] 搞定。
> 项目代码零警告编译通过，已开源到 GitHub。

### 遇到困难怎么说

> 当时的症状是 [具体现象]。我先排查了 [第一轮假设]，发现不是；然后排查了 [第二轮假设]，也不是；最后从 [底层原因] 入手，发现是 [根因]。解决后我总结了 [经验教训]。

**关键：展示排查思路，而不是直接说答案。** 面试官想听的是你的思考过程。
