# 智能桌面气象站 — 开发计划

> 开发环境：STM32F103C8T6 + FreeRTOS(CMSIS_V2) + CubeMX  
> OLED 显示：0.96寸 (软件I2C, SCL=PB6, SDA=PB7)  
> 芯片资源：72MHz, 20KB SRAM, 64KB Flash

---

## 引脚分配一览

| 引脚 | 功能 | 接口 |
|------|------|------|
| PA0 | DHT11 数据线 | 单总线 |
| PA1 | 光敏传感器 | ADC1 |
| PA2/PA3 | ESP8266 TX/RX | USART2 |
| PA6 | TB6612 PWMA | TIM3 CH1 |
| PA7 | TB6612 AIN1 | GPIO |
| PA8 | TB6612 AIN2 | GPIO |
| PA9/PA10 | HC-06 TX/RX | USART1 |
| PA11 | SG-90 PWM | TIM1 CH4 |
| PA15 | W25Q64 CS | SPI1 片选 |
| PB0/PB1 | 编码器 A/B | EXTI |
| PB2 | 编码器 SW | EXTI |
| PB3/PB4/PB5 | W25Q64 SCK/MISO/MOSI | SPI1 |
| PB6/PB7 | OLED SCL/SDA | 软件I2C |
| PB8 | 绿色 LED | GPIO |
| PB9 | 黄色 LED | GPIO |
| PB10/PB11 | MPU6050 SCL/SDA | I2C2 |
| PB12 | KEY1 | GPIO |
| PB13 | KEY2 | GPIO |
| PB14 | 蜂鸣器 | GPIO |
| PC13 | 红色 LED | GPIO |

---

## 开发顺序 — 6 阶段，从简到难

```
阶段1: 传感器 + OLED        ← 先跑通显示（最基础）
阶段2: 编码器 + 按键        ← 加上交互
阶段3: 风扇 + 舵机          ← 加上控制
阶段4: WiFi + 蓝牙          ← 加上无线
阶段5: Flash + 报警         ← 加上存储与安全
阶段6: 全系统集成            ← 联调收尾
```

---

### 阶段 1：传感器 + OLED 显示

**目标：** OLED 实时显示温度/湿度/光照/姿态

**新建文件：**
- `User/Inc/sensor.h` / `User/Src/sensor.c` — DHT11 + 光敏ADC + MPU6050
- `User/Inc/display.h` / `User/Src/display.c` — OLED 4行界面管理

**修改文件：**
- `Core/Src/freertos.c` — 新增 Task_Sensor + Task_Display
- `cmake/stm32cubemx/CMakeLists.txt` — 注册新 .c 文件

**OLED 布局：**
```
行1: 25.3C  65%     (温度 湿度)
行2: Light: 1234    (光敏 ADC)
行3: Gyro: +12 -5   (俯仰 横滚)
行4: Status: OK     (系统状态)
```

**验证方法：** OLED 显示数据，手遮光敏数值变，倾斜板子姿态变

---

### 阶段 2：旋转编码器 + 按键交互

**目标：** 编码器调节阈值，按键切换模式

**新建文件：**
- `User/Inc/encoder.h` / `User/Src/encoder.c` — 编码器 + 按键消抖
- `User/Inc/system_state.h` / `User/Src/system_state.c` — 全局状态结构体

**修改文件：**
- `Core/Src/freertos.c` — 新增 Task_Encoder + Task_Button
- `Core/Src/stm32f1xx_it.c` — EXTI0/1/2 中断处理

**验证方法：** 旋转编码器调阈值(OLED行4显示)，按键切换 AUTO/MANUAL/SLEEP

---

### 阶段 3：风扇 + 舵机控制

**目标：** 温度控风扇(PWM)，光照控舵机

**新建文件：**
- `User/Inc/motor.h` / `User/Src/motor.c` — TB6612 PWM 调速
- `User/Inc/servo.h` / `User/Src/servo.c` — SG-90 角度控制

**修改文件：**
- `Core/Src/freertos.c` — 新增 Task_FanControl + Task_ServoControl

**控制逻辑：**
```
风扇: <26°C 停 | 26~30°C 30% | 30~33°C 60% | ≥33°C 100%
舵机: 光照适中→0° | 强→90° | 极强→180°
```

**验证方法：** 手捏DHT11风扇启动，手电照光敏舵机转动

---

### 阶段 4：WiFi + 蓝牙无线通信

**目标：** ESP8266上传数据，HC-06蓝牙遥控

**新建文件：**
- `User/Inc/esp8266.h` / `User/Src/esp8266.c` — AT指令驱动
- `User/Inc/hc06.h` / `User/Src/hc06.c` — 蓝牙指令解析

**修改文件：**
- `Core/Src/freertos.c` — 新增 Task_ESP8266 + Task_BT
- `Core/Src/stm32f1xx_it.c` — USART1/2 RX 中断

**数据格式：**
```
WiFi上报: {"temp":25.3,"humi":65,"light":1234,"fan":30}
蓝牙指令: "FAN:50" | "SERVO:90" | "MODE:AUTO" | "STOP"
```

**验证方法：** 串口助手测试AT指令，手机蓝牙连接后遥控风扇

---

### 阶段 5：Flash 数据日志 + 报警

**目标：** W25Q64每分钟存记录，异常时蜂鸣器报警

**新建文件：**
- `User/Inc/w25q64.h` / `User/Src/w25q64.c` — SPI Flash 驱动
- `User/Inc/logger.h` / `User/Src/logger.c` — 日志系统
- `User/Inc/alarm.h` / `User/Src/alarm.c` — 多级报警

**修改文件：**
- `Core/Src/freertos.c` — 新增 Task_Logger + Task_Alarm

**日志结构体 (12字节/条)：**
```c
typedef struct {
    uint16_t temp;      // 温度x10
    uint16_t humi;      // 湿度x10
    uint16_t light;     // 光敏值
    int16_t  pitch;     // 俯仰x10
    uint16_t timestamp; // 分钟计数
} LogRecord_t;  // W25Q64 64KB可存 ~5400条
```

**验证方法：** 运行10分钟后提取日志回溯，倾斜板子蜂鸣器响

---

### 阶段 6：全系统集成

**目标：** 所有模块联调，完善界面和保护机制

**修改文件：**
- `Core/Src/freertos.c` — 互斥量保护I2C总线，统一优先级
- `User/Src/display.c` — 4页OLED界面(实时/阈值/日志/系统)

**最终任务表：**
| 任务 | 优先级 | 周期 | 职责 |
|------|--------|------|------|
| Task_Sensor | AboveNormal | 2s | 传感器采集 |
| Task_Display | Normal | 500ms | OLED刷新 |
| Task_FanControl | AboveNormal | 1s | 风扇PWM |
| Task_ServoControl | Normal | 1s | 舵机角度 |
| Task_Logger | Low | 60s | 日志写入 |
| Task_Encoder | Normal | 50ms | 编码器 |
| Task_Button | Normal | 20ms | 按键消抖 |
| Task_ESP8266 | Low | 30s | WiFi上报 |
| Task_BT | Low | 阻塞 | 蓝牙指令 |
| Task_Alarm | High | 事件 | 报警 |

---

## 新增文件清单

```
User/Inc/               User/Src/
sensor.h                sensor.c          DHT11+光敏+MPU6050
display.h               display.c         OLED界面
encoder.h               encoder.c         编码器+按键
system_state.h          system_state.c    全局状态
motor.h                 motor.c           TB6612风扇
servo.h                 servo.c           SG-90舵机
esp8266.h               esp8266.c         WiFi
hc06.h                  hc06.c            蓝牙
w25q64.h                w25q64.c          Flash
logger.h                logger.c          日志
alarm.h                 alarm.c           报警
```
